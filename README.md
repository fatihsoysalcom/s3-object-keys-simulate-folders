# s3-object-keys-simulate-folders
This example demonstrates how Amazon S3, an object storage service, handles "folders." It shows that S3 stores objects with flat keys, and what appears as a hierarchical folder structure is merely a convention based on key prefixes and delimiters. The script uploads objects with nested-looking keys and then lists them to illustrate both the flat ke
